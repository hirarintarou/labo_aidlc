# Practices Discovery — Interview Questions

## Sources

- [scope] Workflow-selected scope: `mvp`.

> 個人開発（1人）・Windows専用・ローカル完結の小規模ツール。開発の進め方・テスト方針・デプロイ・コードスタイルを、あなたの意向に合わせて確定します。技術スタックは未確定なので、具体的なツール名はスタック確定後に決めます。

---

## Q1. 開発の進め方（バージョン管理・レビュー）はどうしますか

個人開発なので、1つのリポジトリで作業ブランチを作り `main` にまとめる、というシンプルな進め方を想定しています。

- A. その方針でよい（1リポジトリ・作業ブランチ→mainへマージ、形式的なPRレビューは省略しセルフ確認）
- B. 個人でも、変更前に必ずセルフレビューのチェックリストを通す運用にしたい
- C. その他の希望がある（記入してください）
- X. Other (please specify)

[Answer]:A.

---

## Q2. 「まず端から端まで動く最小版」を先に作りますか

ウォーキングスケルトンとは、機能を作り込む前に、全体が一通り動く最小の骨組み（例：画像を開く→何もしない加工→非破壊で保存、が通る）を先に作って、部品がつながることを確かめるやり方です。

- A. 作らない。機能を素直に積み上げる（ただし「開く→保存」の最小疎通だけは早めに確認）
- B. 作る。最初に端から端まで通る骨組みを用意してから機能を足す
- C. おまかせ
- X. Other (please specify)

[Answer]:B.

---

## Q3. テストの方針はどうしますか（作る順番）

- A. 実装した後にテストを書く（test-after）。画像処理の中核（補正・保存・入出力）は特に手厚くテストする
- B. 先にテスト/期待動作を決めてから実装する（test-first/TDD）
- C. おまかせ
- X. Other (please specify)

[Answer]:A.

---

## Q4. テストのカバレッジ目標はどうしますか（レビューで論点になりました）

品質担当から「画面(GUI)まで含めて一律80%は個人開発には過大では」という指摘がありました。おすすめは「画像処理の中核ロジック（補正・フィルタ・入出力・保存）に80%を課し、画面まわりは対象外にする」案です。

- A. おすすめ通り：中核ロジックに80%、画面まわりは対象外
- B. アプリ全体に80%（GUI含む）
- C. カバレッジ目標は設けず、重要な処理にテストがあればよい
- D. おまかせ
- X. Other (please specify)

[Answer]:A.

---

## Q5. テストや品質チェックを回す仕組みはどうしますか

- A. コミット/プッシュ前に自動でテストを走らせる仕組み（pre-commit/pre-pushフック等）をローカルに用意する
- B. GitHub Actions などのリモートCIを用意する
- C. 手動でその都度ローカル実行する（仕組みは作らない）
- D. おまかせ
- X. Other (please specify)

[Answer]:A.

---

## Q6. リリース（デプロイ）の形はどうしますか

ローカル完結のデスクトップアプリなので、サーバーへのデプロイではなく「自分用にビルドして使える状態にする」ことをリリースと考えています。

- A. その通り：ローカルでビルドしてインストール/実行できれば完了（サーバー・パイプラインは持たない）
- B. その他の希望がある（記入してください）
- X. Other (please specify)

[Answer]:A.

---

## Q7. コードの構造・エラー処理の原則を決めておきますか（レビューで提案がありました）

開発担当から、mvpでも守ると後が楽になる原則として次が提案されました。おすすめは全部採用です。

1. 画像処理の中核は画面(GUI)に依存させない（中核だけでテストできる）
2. 壊れた画像・非対応形式は早めに検出し、分かりやすい日本語メッセージにして、落ちない
3. 保存は元画像を壊さないよう安全に書き出す（一時ファイル→リネーム等）

- A. 全部採用する（コードスタイル/制約に明記）
- B. 一部だけ採用する（どれを採用するか記入）
- C. どれも今は決めない（おまかせ）
- X. Other (please specify)

[Answer]:A.

---

## Q8. 依存ライブラリ・画像ファイルの安全性について（レビューで提案がありました）

セキュリティ担当から、ローカルツールでも意味がある軽い対策として次が提案されました。

1. 使うライブラリはバージョンを固定し、ロックファイルをコミットする（怪しい類似名パッケージに注意）
2. 読み込む画像は「未検証データ」として扱い、標準ライブラリで安全に開く（壊れた画像で落ちない）

- A. 両方を方針として採用する（制約に明記）
- B. 一部だけ採用する（記入）
- C. 今は決めない（おまかせ）
- X. Other (please specify)

[Answer]:A.

---

## Consolidated Summary Confirmation

- Looks correct
- Request changes

[Answer]: Looks correct
