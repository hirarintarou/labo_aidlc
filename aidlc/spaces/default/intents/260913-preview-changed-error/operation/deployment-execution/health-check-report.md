# 健全性チェックレポート — preview-changed-error（bugfix）

## 対象

ローカルデスクトップアプリ `screenshot-editor`（本 bugfix 反映後）。

## チェック項目と結果

| チェック | 方法 | 結果 |
|---|---|---|
| import 健全性 | `import screenshot_editor.ui.panels` | OK（例外なし） |
| 自動テスト | `pytest -q`（ヘッドレス） | 55/55 PASS |
| カバレッジ（core/io） | `pytest --cov` | 90%（下限 80% 超過） |
| lint / format | `ruff check .` / `black --check .` | clean |
| シグナル配線スモーク | ヘッドレスで調整・プリセット・reset を駆動 | コンソールエラーなし（PASS） |
| 依存の健全性 | `pyproject.toml` / `uv.lock` のピン留め | 維持 |

## 総合判定

**健全（Healthy）**。報告バグは解消され、既存機能に退行なし。ローカル利用可能な状態。

## 監視・メトリクス

- 本アプリはローカル・オフラインのデスクトップアプリであり、常時稼働サービスの SLO/アラートは適用外（team.md / operation phase の運用方針に照らして N/A）。健全性は上記のローカルチェックで代替する。

## 既知の制約

- 実ディスプレイ上での対話的 GUI 起動は本自動チェックの範囲外（ヘッドレスで同等の配線経路を検証済み）。利用者が実機でアプリを起動し、画像を開いて調整・プリセットを操作してコンソールにエラーが出ないことを最終確認することを推奨。
