{
  "version": "v1",
  "hooks": [
    {
      "name": "aidlc-rebuild-stage-graph",
      "trigger": "PostToolUse",
      "matcher": "execute_bash|execute_pwsh|shell",
      "description": "Rebuilds the compiled plan of remaining stages after the workflow moves, so routing decisions use current state.",
      "action": { "type": "command", "command": "powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File \"C:\\Users\\User\\AppData\\Local\\aidlc\\aidlc-shim.ps1\" engine adapter kiro-ide rebuild-stage-graph" }
    }
  ]
}
